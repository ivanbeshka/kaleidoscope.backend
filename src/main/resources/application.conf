ktor {
  deployment {
    port = 8080
    db_path = ${DB_URL}
    path = ${PATH}
  }
  application {
    modules = [ru.kaleidoscope.ApplicationKt.module]
  }
}

jwt {
  secret = ${SECRET}
  realm = "Access to application"
}
